import React, { FC } from 'react';

// Components
import { Container } from '~widgets/Container';

const Leagues: FC = () => {
  return (
    <Container>
      Leagues
    </Container>
   );
}

export default Leagues;