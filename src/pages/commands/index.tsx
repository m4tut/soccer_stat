import React, { FC } from 'react';

// Components
import { Container } from '~shared/layout/Container';

const Commands: FC = () => {
  return (
    <Container>
    </Container>
  );
};

export default Commands;
