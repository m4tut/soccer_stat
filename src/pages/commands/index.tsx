import React, { FC } from 'react';

// Components
import { Container } from '~widgets/Container';

const Commands: FC = () => {
  return (
    <Container>
      commands
    </Container>
   );
}

export default Commands;